<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Om du vill styra ikonfärger: -->
        <item name="android:windowLightStatusBar">true</item> <!-- eller false -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <!-- Jetpack SplashScreen (Android 12+) -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/purple_500</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="31">@android:color/transparent</item>
    </style>

</resources>