<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/details_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp">

    <!-- Guideline ~40%: vänsterpanel -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline40"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="0.4" />

    <!-- Vänster: Headerkort -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardHeader"
        style="?attr/materialCardViewElevatedStyle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:contentDescription="@null"
        app:cardUseCompatPadding="true"
        app:cardCornerRadius="20dp"
        app:strokeColor="?attr/colorOutlineVariant"
        app:strokeWidth="1dp"
        app:cardBackgroundColor="?attr/colorSurface"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/guideline40">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="16dp"
            android:paddingBottom="16dp">

            <TextView
                android:id="@+id/txtCity"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceTitleLarge"
                android:textStyle="bold"
                android:includeFontPadding="false"
                android:maxLines="2"
                android:ellipsize="end"
                tools:text="Malmö, Skåne, Sverige" />

            <TextView
                android:id="@+id/txtCurrent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textAppearance="?attr/textAppearanceBodyLarge"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:includeFontPadding="false"
                tools:text="Nu: 18,2 °C • Nästa timme: 30 % regn" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Höger: 5-dagars-listan -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/listHourly"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="10dp"
        android:overScrollMode="always"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/guideline40"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/item_day_summary" />

    <!-- Tomvy -->
    <TextView
        android:id="@+id/emptyView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:textAlignment="center"
        android:alpha="0.9"
        android:includeFontPadding="false"
        android:visibility="gone"
        android:text="@string/no_results"
        app:layout_constraintTop_toTopOf="@id/listHourly"
        app:layout_constraintBottom_toBottomOf="@id/listHourly"
        app:layout_constraintStart_toStartOf="@id/listHourly"
        app:layout_constraintEnd_toEndOf="@id/listHourly" />

    <!-- Progress -->
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progress"
        style="@style/Widget.Material3.CircularProgressIndicator"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/listHourly"
        app:layout_constraintBottom_toBottomOf="@id/listHourly"
        app:layout_constraintStart_toStartOf="@id/listHourly"
        app:layout_constraintEnd_toEndOf="@id/listHourly" />

</androidx.constraintlayout.widget.ConstraintLayout>